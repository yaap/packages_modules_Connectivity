<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.multiproxyhandler">

    <application
        android:label="@string/app_name"
        android:defaultToDeviceProtectedStorage="true"
        android:directBootAware="true">

        <service android:name="com.android.multiproxyhandler.MultiProxyService"
                 android:permission="android.permission.MAINLINE_NETWORK_STACK"
                 android:exported="true">
            <!-- This intent is only used to find this service via PackageManager. -->
            <intent-filter>
                <action android:name="com.android.server.connectivity.intent.service.MULTI_PROXY_SERVICE" />
            </intent-filter>
        </service>
    </application>
</manifest>